// Stub for the WASM srs_bindings package — used only in the vitest environment.
// The real binary lives at src/lib/srs_bindings/ and is gitignored.
export default async function init() {}
export class SrsRepository {}

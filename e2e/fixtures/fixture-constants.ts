/**
 * fixture-constants.ts — stable IDs from the muSrs.srsj test fixture.
 *
 * These are fixture assertion values, NOT application constants. They exist only
 * to verify round-trip correctness in e2e tests (e.g., that the WASM rendered the
 * expected view). If the fixture is regenerated, update these values here.
 */

/** The guide-body document-view ID in the muSrs.srsj fixture. */
export const FIXTURE_GUIDE_VIEW_ID = "2aba4d85-317b-44e1-a600-d38a743b4cb4";

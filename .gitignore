node_modules/
dist/
.svelte-kit/
.env
.env.*
!.env.example
# committed deliberately: public browser identifiers only (client IDs, picker
# API key) so isolated builds (Cloudflare Workers Builds, CI) bake them in.
# Provider client SECRETS are Worker secrets — never in this file.
!.env.production

# wrangler local dev secrets + state for the OAuth proxy Worker
.dev.vars
!.dev.vars.example
.wrangler/

# WASM build output — built from srs-rust, not committed here
src/lib/srs_bindings/
pkg/

# Editor
.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Playwright
test-results/
playwright-report/
.playwright/

# transient e2e round-trip artifacts
e2e/fixtures/_*.srsj
